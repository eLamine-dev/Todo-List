import BaseModel from './BaseModel';

class ProjectModel extends BaseModel {
   constructor() {
      super();
   }

   updateStatus() {
      // if all project tasks are finished mark it as completed projects
   }
}

export default ProjectModel;
